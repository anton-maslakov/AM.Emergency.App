@model IEnumerable<AM.EmergencyService.App.Common.Models.Authorization.RoleModel>
<table class="table">
@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Rolename)
        </td>
    </tr>
}

</table>
